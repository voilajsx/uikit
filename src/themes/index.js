/**
 * @fileoverview Theme system exports
 * @description Simplified theme system exports
 * @package @voilajsx/uikit
 * @file /src/themes/index.js
 */

// Export the theme provider and hook
export { ThemeProvider, useTheme } from './theme-provider';

// That's it! Everything else is handled by the build process and CSS
